class BusStop {
    constructor(x, y, width, height) {
        // Initialisierung der Bushaltestelle
        this.x = x;
        this.y = y;
        this.width = width;
        this.height = height;
        // Weitere Eigenschaften und Logik für die Bushaltestelle
    }

    draw() {
        // Zeichnen der Bushaltestelle auf dem Bildschirm
    }

    checkCollision(player) {
        // Kollisionsprüfung des Spielers mit der Bushaltestelle
    }
}